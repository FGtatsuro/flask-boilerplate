#!/usr/bin/env python
# -*- coding: utf-8 -*-

import os
from {{app_name}} import create_app

if __name__ == '__main__':
    create_app(None).run(
        host='0.0.0.0',
        port=int(os.environ.get('PORT', 5000)))
